/*
■問題
関数「pow」が定義されています。関数「pow」は引数を1個受け取り、引数の数値を「2乗」した結果を返します。
関数「pow」を利用して、実行結果のように表示する処理を完成させてください

※　この問題は関数の呼び出し方法の問題です。関数「pow」を修正してはいけません
※　関数「pow」の戻り値を結果として表示します

■実行結果
25
*/

// 関数を修正してはいけません
// 関数はこのままの形で利用します
function pow(a) {
    return a * a;
}

// ここより下側に関数「pow」を呼び出す処理を作成します

